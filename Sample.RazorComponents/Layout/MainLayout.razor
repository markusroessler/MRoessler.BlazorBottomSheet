@using Microsoft.AspNetCore.Components.Sections
@inherits LayoutComponentBase

<MudThemeProvider @ref="_themeProvider" Theme="@_theme" IsDarkMode="_isDarkMode" ObserveSystemThemeChange=@(_currentThemeIcon == AutoThemeIcon) />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />
<BottomSheetOutlet ColorScheme="@GetColorScheme()" />

@if(_isVisible) {
    <MudLayout>
        <MudAppBar Elevation="1">
            <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
            <MudText Typo="Typo.h5" Class="ml-3">
                <SectionOutlet SectionName="@AppBarHeaderName" />
            </MudText>

            <MudSpacer />
            
            <MudMenu Label="Theme" EndIcon="@_currentThemeIcon" AnchorOrigin="Origin.BottomRight" TransformOrigin="Origin.TopRight" data-testid="theme-menu">
                <MudMenuItem Label="Auto" Icon="@AutoThemeIcon" OnClick="()=>SetCurrentThemeIcon(AutoThemeIcon)" data-testid="auto-theme-menuitem" />
                <MudMenuItem Label="Light" Icon="@LightThemeIcon" OnClick="()=>SetCurrentThemeIcon(LightThemeIcon)" data-testid="light-theme-menuitem" />
                <MudMenuItem Label="Dark" Icon="@DarkThemeIcon" OnClick="()=>SetCurrentThemeIcon(DarkThemeIcon)" data-testid="dark-theme-menuitem" />
            </MudMenu>
        </MudAppBar>

        <MudDrawer id="nav-drawer" @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
            <NavMenu />
        </MudDrawer>

        <MudMainContent Class="pt-16 pa-4" Style="height: 100%;">
            @Body
        </MudMainContent>
    </MudLayout>
}


<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

